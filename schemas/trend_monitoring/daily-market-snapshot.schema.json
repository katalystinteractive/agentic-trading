{
  "schema_version": 1,
  "artifact": "daily-market-snapshot",
  "required": [
    "schema_version",
    "artifact_type",
    "as_of_date",
    "generated_at",
    "records"
  ],
  "record_required": [
    "ticker",
    "metrics",
    "source_refs"
  ],
  "enums": {},
  "notes": "M1 adds keyed `tickers` object + universe/cache_status/provider_failures roots; `records` list retained for downstream tools."
}
